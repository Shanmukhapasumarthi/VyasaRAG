# --- PDF handling + OCR (data_loader.py) ---
pymupdf
pytesseract
pillow

# --- Embeddings (embedding.py — BGE-M3) & Reranker (search.py — CrossEncoder) ---
sentence-transformers

# --- Tokenizer for chunk sizing (chunking.py — must match embedding model) ---
transformers
protobuf==6.31.1

# --- Vector store (vector_store.py) ---
chromadb

# --- LLM generation & Query Expansion (translate_text.py / search.py — Groq) ---
groq

# --- Web framework & Templates (app.py) ---
fastapi
uvicorn[standard]
jinja2

# --- Config / env management ---
python-dotenv

# --- Language detection (translate_text.py) ---
langdetect

# --- Hybrid search — BM25 keyword index (bm25_index.py + search.py) ---
rank-bm25
numpy