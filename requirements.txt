# --- PDF handling + OCR (data_loader.py) ---
pymupdf>=1.24.0
pytesseract>=0.3.10
pillow>=10.0.0

# --- Embeddings (embedding.py — BGE-M3) ---
sentence-transformers>=2.7.0

# --- Tokenizer for chunk sizing (chunking.py — must match embedding model) ---
transformers>=4.40.0

# --- Vector store (vector_store.py) ---
chromadb>=0.5.0

# --- LLM generation (translate_text.py / search.py — Groq) ---
groq>=0.9.0

# --- Web framework (app.py / main.py) ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# --- Config / env management ---
python-dotenv>=1.0.0

# --- Language detection (translate_text.py) ---
langdetect>=1.0.9

# --- Core utility ---
numpy>=1.26.0